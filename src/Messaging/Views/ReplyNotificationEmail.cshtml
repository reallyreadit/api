@model api.Messaging.Views.ReplyNotificationEmailViewModel

@{
	Layout = "EmailLayout";
	var footerColorStyle = "color: darkgray;";
}

<p>@Model.Respondent just replied to your comment re: @Model.ArticleTitle</p>
<p>Click <a href="@Model.WebServerEndpoint.CreateUrl("/viewReply?token=" + Model.ReplyToken)">here</a> to view the reply in the comment thread.</p>

@section UnsubscribeNotice {
	<p style="@footerColorStyle">
		You are receiving this email because you are subscribed to comment reply notifications on reallyread.it.<br />
		<a href="@Model.WebServerEndpoint.CreateUrl("/email/subscriptions?token=" + Model.SubscriptionsToken)" style="@footerColorStyle">Manage your email subscriptions / Unsubscribe</a>
	</p>
}